apiVersion: v1
kind: Service
metadata:
  name: portfolio-next
  labels:
    app: portfolio-next
spec:
  selector:
    app: portfolio-next
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
